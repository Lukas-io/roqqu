import 'package:flutter/material.dart';

class NewCoinsScrollView extends StatelessWidget {
  const NewCoinsScrollView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
